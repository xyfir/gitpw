export async function lockCommand(): Promise<void> {
  // Grab files from outside of .gitpw/
  // Get mapping from .gitpw/map.json
  // Encrypt each file to copy to .gitpw
  // Update .gitpw/map.json with new mapping
  return;
}
